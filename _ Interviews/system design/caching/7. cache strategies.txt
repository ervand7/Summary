## 1️⃣ Cache-aside (lazy loading) — **самая популярная**
```
Read:
cache → miss → DB → cache

Write:
DB → delete cache
```
👉 Приложение само управляет кэшем
👉 Используется в 80% систем


## 2️⃣ TTL-based (expiration)
```
SET user:1 {...} TTL=60s
```
👉 Кэш сам протухает
👉 Всегда есть небольшая устарелость


## 3️⃣ Explicit invalidation on write
```
UPDATE users SET ...
DEL cache user:1
```
👉 Точнее, чем TTL
👉 Легко забыть → баги


## 4️⃣ Write-through cache
```
Write:
cache ← data
DB    ← data
```
👉 Кэш всегда свежий
👉 Записи медленнее


## 5️⃣ Write-back / write-behind
```
Write:
cache ← data
DB ← async
```
👉 Очень быстро
👉 Потеря данных = катастрофа


## 6️⃣ Read-through cache
```
cache.get(key)
  → cache сам идёт в DB
```
👉 Логика спрятана в кэше
👉 Приложение проще


## 7️⃣ Versioned keys (cache busting)
```
user:1:v1
user:1:v2
```
👉 Старый кэш сам умирает
👉 Нужно управлять версиями


## 8️⃣ Two-level cache (local + shared)
```
local cache → Redis → DB
```

👉 Очень быстро
👉 Сложнее в поддержке


## 9️⃣ Negative caching
```
user:999 = NOT_FOUND (TTL 30s)
```

👉 Защищает БД от повторных miss’ов


## 10️⃣ Probabilistic early refresh
```
Обновить кэш ДО истечения TTL
```
👉 Борется с cache stampede
👉 Используется в хайлоаде
