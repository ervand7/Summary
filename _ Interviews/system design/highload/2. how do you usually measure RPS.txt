RPS = requests per second

Example:
10,000 requests in 1 second = 10k RPS


## How it’s measured
Usually:
* count requests for some time window
* divide by time

Examples:
* 1s → noisy
* 10s → common
* 1m → stable


## Prometheus example
```go
rate(http_requests_total[1m])