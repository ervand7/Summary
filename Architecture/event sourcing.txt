### 📌 Суть:

1. Вместо того чтобы хранить **состояние** (например, текущие балансы банковских счетов),
мы храним **все события (transactions)**, которые привели к этому состоянию.
2. Состояние можно получить в любой момент, просто **переиграв все события с начала времени**.
3. Это позволяет:

   * Избежать **изменяемых данных (mutable state)**,
   * Сделать данные **неизменяемыми (immutable)**,
   * Избежать проблем с **параллельными изменениями (concurrency)**.


### 📋 Примеры:
* Вместо хранения «Баланс = \$100» → храним:
  * +\$50
  * +\$30
  * +\$20
* Чтобы узнать баланс → суммируем все события.


### 🔑 Почему это возможно сейчас:

* Хранилища стали огромными и дешёвыми,
* Процессоры стали быстрыми,
* Поэтому можно позволить себе **иммутабельные архитектуры**.


✅ В двух словах:
**Event Sourcing** = вместо сохранения текущего состояния → сохраняем все события → восстанавливаем состояние при необходимости.
Похоже на работу **git**.
