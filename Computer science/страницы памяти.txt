🔑 **Страницы памяти** — это маленькие фиксированные блоки оперативной памяти
(обычно 4 КБ), с которыми работает операционная система для управления памятью.

✅ Они используются для:
* реализации **виртуальной памяти**,
* защиты процессов друг от друга,
* выгрузки неиспользуемых данных на диск (swap).

👉 Страница может находиться либо в **RAM**, либо временно на **диске**.
