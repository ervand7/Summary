In DynamoDB, access patterns = the exact queries your app must support.

You design keys first, data second.
If you can’t express a query with PK / SK (or GSI) → it’s the wrong pattern for DynamoDB.

👉 DynamoDB is *query-driven*, not schema-driven.
