Когда горутина сталкивается с блокирующей операцией (например, `val := <-ch`),
она переходит в состояние:

### 🟡 `waiting` (ожидание)
* Её состояние меняется на **`_Gwaiting`**
* Она помещается в очередь ожидания канала (например, `recvq` для чтения)
* **Процессор `P` освобождается**, и планировщик может запустить другие горутины

Когда операция разблокируется (например, другой `G` отправил в канал):

* Горутина пробуждается, становится **`_Grunnable`**
* Помещается обратно в очередь выполнения (`runq`)
* Планировщик снова может запустить её

---

Состояния горутины в Go runtime:

| Состояние    | Что значит            |
| ------------ | --------------------- |
| `_Grunnable` | Готова к запуску      |
| `_Grunning`  | Сейчас выполняется    |
| `_Gwaiting`  | Ожидает (блокирована) |
| `_Gdead`     | Завершена             |
