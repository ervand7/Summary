🔑 **Регистры ЦП (CPU Registers)** — это сверхбыстрая память внутри процессора,
которая используется для выполнения операций и хранения промежуточных данных.

✅ Регистры работают значительно быстрее, чем оперативная память и обеспечивают
эффективное выполнение инструкций.


### Основные типы регистров:

1. **Регистры общего назначения (General-Purpose Registers):**
   Хранят данные и адреса во время вычислений, используются для арифметики, логики
   и работы с памятью.

2. **Счётчик команд (Program Counter, PC):**
   Хранит адрес следующей инструкции. Автоматически увеличивается после выполнения каждой команды.

3. **Регистр флагов (Flags Register):**
   Хранит флаги состояния (переполнение, ноль, перенос и др.) для контроля выполнения инструкций.

4. **Регистры сегментов (Segment Registers):**
   Используются в некоторых архитектурах (например, x86) для адресации памяти через сегменты.

5. **Регистры стека (Stack Registers):**
   Обслуживают стек вызовов и локальные переменные, управляют переходами между функциями.

6. **Специализированные регистры (Special-Purpose Registers):**
   Управляют прерываниями, памятью, аппаратной синхронизацией и другими специальными задачами.
