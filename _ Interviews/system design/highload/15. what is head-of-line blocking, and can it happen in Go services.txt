One slow request blocks faster requests behind it.

Result:
* requests wait in a queue
* latency increases
* throughput may drop

Where it happens:
* HTTP/1.1 connections
* shared locks
* channels
* worker pools

HTTP/2 helps because:
* many requests can run over one connection

> HoL blocking = one slow operation delays many others.