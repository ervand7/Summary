Вот обновлённый и расширенный конспект:

🔑 **Транзистор** — это полупроводниковое устройство для **управления током** или
**усиления сигналов**. У него три вывода:

1. **Коллектор (Collector)** — собирает ток, обычно подключён к плюсу питания.
2. **База (Base)** — управляет прохождением тока через транзистор.
3. **Эмиттер (Emitter)** — источник носителей заряда (электронов или дырок).

✅ Маленький ток в базу → управляет большим током между эмиттером и коллектором.
✅ Это основа первых **цифровых схем 50–60-х годов**.
❗ Эти транзисторы — **биполярные (BJT)** — сейчас считаются устаревшими для процессоров
и микросхем.

        Коллектор (C)
             │
             │
            ─┴─
           │ B │  ← База (Base)
            ─┬─
             │
             │
        Эмиттер (E)


🔑 **Современные транзисторы** — это **полевые транзисторы (MOSFET)**, которые
применяются во всех современных процессорах (включая iPhone, Intel, AMD).

У них три других вывода:
1. **Затвор (Gate)** — управляет включением/выключением тока (аналог базы).
2. **Сток (Drain)** — куда уходит ток.
3. **Исток (Source)** — откуда ток приходит.

✅ Эти транзисторы работают без протекания тока через управляющий электрод (затвор).
✅ Размеры — **нанометры** (миллиарды штук на одном кристалле).

        Сток (D)
           │
           │
          ─┴─
         │ G │  ← Затвор (Gate)
          ─┬─
           │
           │
       Исток (S)


👉 **Биполярные транзисторы (BJT)** → 50–60-е годы, устарели.
👉 **Полевые транзисторы (MOSFET)** → современные процессоры, нанометровые технологии.
