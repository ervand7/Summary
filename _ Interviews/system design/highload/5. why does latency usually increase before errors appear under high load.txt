Requests first start waiting on:
* CPU
* DB
* locks
* connections

So the system still works, just slower.

Higher latency →
more in-flight requests →
even more slowdown.

Errors appear later when:
* timeouts happen
* pools are exhausted
* memory/GC pressure becomes too high

> Latency is the first warning sign.
> Errors come later.