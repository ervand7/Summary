 - с помощью не рекурсивной сортировки (варианты 1 и 2) мы можем слить 2 предварительно
отсортированных списка за O(n)

 - с помощью рекурсивной (пример 3) можем слить с сортировкой 2 предварительно
 не отсортированных списка за O(n * log(n))

