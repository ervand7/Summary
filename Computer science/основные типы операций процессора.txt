🔑 **Процессор умеет выполнять 4 основных типа операций:**


### 1️⃣ **Арифметические операции (Arithmetic)**
* Это: **сложение, вычитание, умножение, деление**.
* Инструкции: `ADD`, `SUB`, `MUL`, `DIV`.
* Используются для числовых вычислений.

✅ Пример: `x = a + b`


### 2️⃣ **Логические (булевы) операции (Logical/Bitwise)**
* Это: **AND (И), OR (ИЛИ), XOR (Исключающее ИЛИ), NOT (НЕ)**.
* Инструкции: `AND`, `OR`, `XOR`, `NOT`.
* Работают побитово с двоичными числами.

✅ Пример: проверка флагов, масок.


### 3️⃣ **Операции сравнения (Comparison)**
* Это: сравнение чисел или значений → устанавливаются **флаги** (больше, меньше, равно).
* Инструкции: `CMP` (compare), `TEST` (bit test).
* Используются для принятия решений в коде.

✅ Пример: `if (a > b)`


### 4️⃣ **Управляющие (переходы и вызовы) (Control/Branch)**
* Это: **переходы по условиям и без, вызовы и возвраты функций**.
* Инструкции: `JMP`, `JE`, `JNE`, `CALL`, `RET`.
* Создают **циклы, условия, вызовы функций**.

✅ Пример: `for`, `if`, `func()`


### Дополнительно:

5️⃣ **Операции с памятью (Memory Access)**
* Это: чтение и запись данных.
* Инструкции: `LOAD`, `STORE`, работа с указателями.
* Крайне важны для любых программ.

✅ Пример: чтение переменной из оперативной памяти.


### 🔑 В итоге:

| Вид операции        | Что делает                               |
| ------------------- | ---------------------------------------- |
| Арифметика          | Числовые вычисления                      |
| Логика              | Побитовые булевые операции               |
| Сравнение           | Выявляет отношения между значениями      |
| Переходы/Управление | Управляет порядком выполнения инструкций |
| Работа с памятью    | Чтение/запись данных в память            |


👉 Всё, что делает процессор — это комбинации этих простых типов операций.
