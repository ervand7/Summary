`raise`

* пробрасывает текущее исключение
* сохраняет оригинальный traceback

`raise e`

* выбрасывает исключение заново
* перезаписывает traceback (ошибка “как будто” здесь)

Правило:

> В `except` почти всегда используй `raise`, а не `raise e`.
