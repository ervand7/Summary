🔑 **Кэш L1, L2 и L3** — это уровни быстрой памяти внутри процессора, которые
ускоряют доступ к данным и инструкциям.

### Уровни кэша:
1. **L1 Cache** — самый быстрый и маленький, находится прямо внутри ядра.
   * Делится на **L1i** (инструкции) и **L1d** (данные).
2. **L2 Cache** — больше, чуть медленнее, может быть отдельным для ядра или общим для
нескольких ядер.
3. **L3 Cache** — самый большой и медленный, общий для всех ядер процессора.


✅ Чем выше уровень (L1 → L3), тем больше объём, но выше задержка.
✅ Все уровни работают вместе, чтобы минимизировать обращения к медленной оперативной памяти.
