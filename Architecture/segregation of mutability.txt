**Segregation of Mutability** — это принцип:
👉 **Отделяй код, который не меняет данные (immutable), от кода, который меняет
данные (mutable).**


### 📌 Пример:
1. 📖 Чтение баланса → **immutable** (ничего не меняем).
2. 💰 Перевод денег → **mutable** (меняем данные → нужен контроль).


✅ Почему важно:
* Immutable код → безопасный и надёжный.
* Mutable код → может сломаться при одновременных изменениях → нужен контроль
(блокировки, транзакции).


✅ Как отделять:
👉 **Разделяй код физически** — в **разные файлы, модули, классы или даже микросервисы**.
* Чтение → в одном месте.
* Изменение → в другом.

Чем меньше mutable-кода → тем проще и стабильнее система.
