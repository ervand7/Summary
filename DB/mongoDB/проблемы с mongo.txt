Проблема:
Вот сейчас настраиваю личный ноут для работы и вот на что наткнулся.
Ты случаем не знаешь, как решить такую проблему?
Когда я на рабочем ноуте в терминале запускаю
kubectl port-forward svc/mongo 27017:27017 --namespace stage
у меня появляются 2 такие строки
Forwarding from 127.0.0.1:27017 -> 27017
Forwarding from [::1]:27017 -> 27017

А когда я запускаю на личном ноуте ну же команду в терминале, то появляется только 1 строка
Forwarding from [::1]:27017 -> 27017
а ожидаемая
Forwarding from 127.0.0.1:27017 -> 27017
которая должна быть перед ней, не появляется.
Мне кажется, что и из-за этого я на личном ноуте не могу зайти в монгодбКомпас, чтобы БД видеть в GUI. Точнее я туда захожду, но БД Start там не вижу.

Причем у меня на личном ноуте нет таких проблем при запуске аналогичной команды в терминале касательно постгреса и редиса. Там появляются по 2 строчки.

Если что, то новый конфиг файл в папку .kube я закинул на личном ноуте.
И, кстати, когда вообще вырубаю соединение в терминале через control+c, то эффект абсолютно такой же

Ответ:
 - у тебя локальная монга запущена

Now let’s see everything we’ve loaded:
● brew services list

For checking MongoDB listening on 27017
● lsof -nP -iTCP:27017

If you installed mongodb with homebrew, there's an easier way:
List mongo job with launchctl:
● launchctl list | grep mongo


● launchctl stop homebrew.mxcl.mongodb-community