🔑 **Кэш-строка (Cache Line)** — это минимальная единица хранения данных в
кэше процессора.

✅ Когда процессор загружает данные из памяти, он берёт не отдельный байт,
а целую **кэш-строку** (например, 64 байта).
✅ Даже если нужна только часть данных, вся строка попадает в кэш — это ускоряет
доступ к соседним данным (эффект пространственной локальности).


### Особенности:
* Размер обычно: **32–128 байт** (часто 64 байта).
* Использование кэш-строк снижает задержки и повышает производительность.


👉 Кэш быстрее RAM, потому что работает с целыми строками, которые хранятся
рядом с процессором.
