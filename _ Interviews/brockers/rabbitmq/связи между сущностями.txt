### 1. Producer → Exchange
* Один producer → один exchange
* Один producer → несколько exchanges ✅
* Много producers → один exchange ✅

📌 Producer всегда пишет в exchange, никогда напрямую в queue.


### 2. Exchange → Queue (bindings)
* Один exchange → одна queue ✅
* Один exchange → много queues ✅
* Одна queue ← много exchanges ✅

📌 Связь настраивается через bindings
📌 Exchange может отправить одно сообщение в несколько очередей


### 3. Queue → Consumer
* Одна queue → один consumer ✅
* Одна queue → много consumers ✅ *(competing consumers)*
* Один consumer → одна queue ✅
* Один consumer → много queues ✅

📌 Если несколько consumers читают одну очередь —
сообщения распределяются между ними, не дублируются


### 4. Сколько сущностей может быть одновременно

| Связь                            | Разрешено          |
| -------------------------------- | ------------------ |
| Много producers → одна queue     | ✅ (через exchange) |
| Один producer → много queues     | ✅ (через exchange) |
| Одна queue → много consumers     | ✅                  |
| Один consumer → много queues     | ✅                  |
| Одно сообщение → много queues    | ✅                  |
| Одно сообщение → много consumers | ❌                  |


### 5. Гарантии доставки (важно понимать связи)
* Очередь → consumer: сообщение получает ровно один consumer
* Fanout / topic exchange: сообщение может попасть в несколько очередей
* Каждая очередь — своя копия сообщения


### 6. Приоритет и порядок
* Producer задаёт `priority` ✔
* Exchange не участвует ❌
* Queue решает порядок выдачи ✔
* Consumers просто получают готовый поток ✔


## Итог в 3 строках (идеально для ответа)
> В RabbitMQ продюсеры публикуют сообщения в exchanges, которые маршрутизируют их в одну
или несколько очередей.
> Очередь может обслуживаться одним или несколькими consumers по модели competing consumers.
> Одно сообщение всегда обрабатывается одним consumer из конкретной очереди.
