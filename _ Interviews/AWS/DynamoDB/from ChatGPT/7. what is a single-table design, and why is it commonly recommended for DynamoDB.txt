> Single-table design — это когда в одной таблице хранят разные типы данных так,
чтобы всё нужное доставалось одним запросом по ключу, без JOIN-ов,
потому что в DynamoDB их нет.
За счёт этого запросы быстрые и система хорошо масштабируется.