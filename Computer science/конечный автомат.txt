🔑 **Конечный автомат** (*Finite State Machine, FSM*) — это математическая модель,
которая описывает поведение системы через **ограниченное число состояний** и **переходы
между ними** в ответ на входные сигналы.

👉 В каждый момент времени система находится в **одном состоянии** и может менять
состояние при получении **входа**.


### 5 основных компонентов конечного автомата:

| Компонент                   | Что это означает                                          | Пример (Светофор)          |
| --------------------------- | --------------------------------------------------------- | -------------------------- |
| **Q (States)**              | Конечное множество **состояний**                          | Красный, Жёлтый, Зелёный   |
| **Σ (Input Alphabet)**      | Множество возможных **входных символов** (событий)        | Таймер, кнопка             |
| **δ (Transition Function)** | **Функция перехода**: определяет, куда перейти по входу   | Красный + Таймер → Зелёный |
| **q₀ (Initial State)**      | **Начальное состояние**                                   | Красный                    |
| **F (Final States)**        | Множество **финальных состояний** (не всегда обязательно) | (например, «Отключен»)     |


✅ Важные моменты:

* Система всегда знает, в каком **состоянии** она находится.
* Переходы всегда зависят от **входа** и **текущего состояния**.
* В любой момент времени активным может быть только **одно состояние**.


### Классический пример — Светофор:
1. **Q** = {Красный, Жёлтый, Зелёный}
2. **Σ** = {таймер сработал}
3. **δ** =

   * Красный + таймер → Зелёный
   * Зелёный + таймер → Жёлтый
   * Жёлтый + таймер → Красный
4. **q₀** = Красный
5. **F** = (может отсутствовать)
