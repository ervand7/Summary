DynamoDB Streams — это поток событий о изменениях в таблице
(INSERT / UPDATE / DELETE).
События идут по порядку внутри одного partition key и доставляются
как минимум один раз, поэтому обработка должна быть идемпотентной.
Используются для асинхронной обработки, репликации и event-driven логики.