### 🔑 Что такое **«слово»** (англ. *word*) в компьютерной архитектуре:

* Это **единица данных**, с которой процессор умеет работать за **один раз**.
* Размер **слова** зависит от **разрядности процессора**.


### Примеры:

| Разрядность процессора | Размер «слова»    |
| ---------------------- | ----------------- |
| 8 бит                  | 8 бит (1 байт)    |
| 16 бит                 | 16 бит (2 байта)  |
| 32 бит                 | 32 бита (4 байта) |
| 64 бит                 | 64 бита (8 байт)  |


✅ Важно:

* Когда Таненбаум говорит: **«команда использует слово из памяти»**, это значит:

  * Процессор считывает или записывает данные размером в одно «слово» (например, 32
  бита или 64 бита) из памяти.
* Слово — это как «шаг» или «порция данных» для конкретной архитектуры.

---

### 📌 Почему это важно:

* Процессор обычно быстрее обрабатывает данные ровно в размере «слова».
* Память может быть организована именно по словам.


✅ Коротко:

* **Слово** = базовый размер данных, с которым работает процессор (например, 32 бита
или 64 бита).
* «Взять слово из памяти» = считать данные именно такого размера.
