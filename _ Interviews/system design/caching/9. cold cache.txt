👉 Cold cache — это кэш, который пустой или почти пустой.

* После старта сервиса
* После рестарта кэша
* После очистки / eviction

Одна строка для интервью:
> Cold cache — это состояние кэша без прогретых данных, когда почти все запросы идут в БД.
