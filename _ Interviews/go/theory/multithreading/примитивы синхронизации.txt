1) sync.Mutex
2) sync.RWMutex — мьютекс, который блокирует только операцию записи,
читать могут несколько горутин одновременно
3) sync.WaitGroup
4) sync.Once - благодаря sync.Do(someFunc()) someFunc исполнится только 1 раз, даже
если он прописан в цикле горутин
5) sync.Pool - потокобезопасный пулл для хранения уже использованных объектов,
чтобы лишний раз не нагружать GC
6) каналы
7) errgroup
8) потокобезопасная мапа
9) sync.Cond — переменная условия, которая останавливает горутину до получения сигнала
10) semafore
11) singleflight
