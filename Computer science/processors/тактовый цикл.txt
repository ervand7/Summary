🔑 **Тактовый цикл (такт)** — это базовый ритм работы процессора, который
синхронизирует выполнение инструкций и операций.


### Ключевые понятия:
1. **Тактовая частота (Clock Speed):**
   Количество тактов в секунду (например, 3 ГГц = 3 млрд тактов/сек).

2. **Время такта:**
   Чем выше частота → тем короче один такт.

3. **IPC (Instructions Per Cycle):**
   Сколько инструкций процессор выполняет за один такт (бывает больше одной).

4. **Производительность:**
   Зависит от частоты × IPC.



✅ Вывод:
Быстрый процессор = высокая частота + высокая эффективность (IPC), но это всегда
баланс между скоростью, энергопотреблением и теплом.
