## ✅ **MVCC = Multi-Version Concurrency Control**

**MVCC** — это **механизм**, с помощью которого СУБД (например, PostgreSQL) позволяет
**нескольким транзакциям одновременно читать и писать**, **без блокировок**.


## 🧠 Основная идея:
Вместо блокировки строк база данных **хранит несколько версий** одной и той же строки.
* Читатели видят **снимок данных (snapshot)** на момент начала запроса или транзакции.
* Писатели создают **новые версии строк**, не затирая старые.


## 🔧 Связь с уровнями изоляции
> **MVCC — это реализация**,
> а **уровень изоляции — это настройка**, определяющая, **какие версии строк ты видишь**
> и **какие аномалии допустимы**.

| Уровень изоляции    | Поведение через MVCC                                          |
| ------------------- | ------------------------------------------------------------- |
| **READ COMMITTED**  | Каждое чтение получает **самую новую подтверждённую версию**  |
| **REPEATABLE READ** | Все чтения в транзакции используют **один и тот же snapshot** |
| **SERIALIZABLE**    | MVCC + дополнительные проверки конфликтов между транзакциями  |


## 📦 Пример:
1. Транзакция A читает строку → видит версию **v1**
2. Транзакция B обновляет строку → создаёт версию **v2**
3. Транзакция A всё ещё видит **v1**, хотя B уже изменила строку
4. Когда A делает коммит, она остаётся согласованной с тем снимком, с которым работала


## 📚 Ключевые понятия

| Понятие                                   | Что это значит                                                       |
| ----------------------------------------- | -------------------------------------------------------------------- |
| **Repeatable reads** (повторяемые чтения) | Все чтения внутри транзакции возвращают одинаковые значения          |
| **No dirty reads** (без грязных чтений)   | Нельзя прочитать **неподтверждённые изменения** из другой транзакции |
| **Phantom reads** (фантомные чтения)      | Можно увидеть новые строки, если не используется `SERIALIZABLE`      |
| **Версионность строк**                    | Обновления создают **новые версии**, старые остаются до VACUUM       |


## 💥 Типовые проблемы
| Проблема                           | Почему это важно                                               |
| ---------------------------------- | -------------------------------------------------------------- |
| **Долгоживущие транзакции**        | Мешают очищать старые версии → ведёт к росту таблицы           |
| **Параллельные обновления**        | Могут привести к `serialization failure` при строгих изоляциях |
| **Read Skew (чтение с перекосом)** | В `READ COMMITTED` два чтения могут вернуть разные значения    |


## ✅ Итоги
* **MVCC** — это **механизм версионности**, который позволяет читать без блокировок.
* **Уровень изоляции** — это **настройка**, управляющая тем, какие версии будут читаться.
* Отлично подходит для систем с **высокой параллельностью**.
* Важно следить за:

  * **долгими транзакциями** (не задерживать VACUUM),
  * **правильным выбором уровня изоляции**,
  * **обработкой ошибок конкурентного доступа**.
