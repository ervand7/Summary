At low load:
* locks are fast
* contention is rare

Under high load:
* many goroutines wait for the same lock
* goroutines block
* throughput stops growing
* latency increases

Common problem:
* one global mutex on a hot path

Main issue:
* only one goroutine can work at a time

> High mutex contention reduces parallelism and scalability.