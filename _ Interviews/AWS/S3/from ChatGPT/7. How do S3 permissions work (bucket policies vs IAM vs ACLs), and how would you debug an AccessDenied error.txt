* IAM policies → grant permissions to identities (users, roles, services)
* Bucket policies → grant permissions to principals on specific buckets/objects
* ACLs → legacy, object-level permissions (generally avoid)


> Most S3 access issues come from missing `ListBucket`, wrong ARNs, or hidden DENY rules—not
from ACLs.
