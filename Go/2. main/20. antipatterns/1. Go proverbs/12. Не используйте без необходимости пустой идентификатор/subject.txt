Часто в цикле или при присваивании не требуется указывать пустой
идентификатор. Лучше избегать его использования.

/* плохо */
v,_ := names[key]

for key,_ := range names {
}

_ = F()
_ = <- ch

/* хорошо */
v := names[key]

for key := range names {
}

F()
<- ch