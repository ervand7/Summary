👉 Данные хранятся на брокерах.

* Partition = лог данных
* Partition физически лежит на broker’е (как файлы на диске)
* Replica — это копия partition на другом broker’е
* Kafka = кластер брокеров, а не отдельный сервер

✔️ Топики и partition — логическая абстракция
✔️ Брокеры — физическое хранилище
