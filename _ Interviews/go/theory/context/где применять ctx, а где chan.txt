Context стоит применять, если хотим отменить что-то в текущей горутине,
например в функции `b`, вызванной в функции `a`. В таком случае, в функции
`a` мы вызываем cancel, а в функции `b` через select-case проверяем <-ctx.Done()



