Use environment variables or SSM Parameter Store for config.

Rule of thumb:

* Secrets (passwords, tokens) → Secrets Manager
* Config (flags, URLs, limits) → Env vars / SSM Parameter Store

Examples:

* `DB_HOST`, `TIMEOUT_MS` → env vars
* Feature flags, non-sensitive config → SSM
* `DB_PASSWORD`, `API_TOKEN` → Secrets Manager

👉 Config ≠ secret.
