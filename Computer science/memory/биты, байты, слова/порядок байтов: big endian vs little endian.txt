🔑 **Little Endian** и **Big Endian** — это два способа, как компьютер
**записывает байты многобайтового числа в память**.


### 1️⃣ **Little Endian**:
* **Младший байт (Least Significant Byte)** пишется первым (по младшему адресу).
* Пример (число `0x12345678`):

```
78 56 34 12
```
✅ Используется в Intel (x86, x86-64), ARM.


### 2️⃣ **Big Endian**:
* **Старший байт (Most Significant Byte)** пишется первым (по младшему адресу).
* То же число `0x12345678`:

```
12 34 56 78
```
✅ Используется в сетевых протоколах (TCP/IP).


### Ключевая идея:
* Это просто **порядок байтов** в памяти.
* **Вычисления внутри CPU** не зависят от порядка — важен только для передачи
или чтения данных.


👉 Запомни коротко:
* **Little** — младший байт впереди (Intel).
* **Big** — старший байт впереди (Network).
