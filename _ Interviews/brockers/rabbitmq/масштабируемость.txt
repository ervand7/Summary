* Каждый инстанс приложения = consumer
* Нет consumer group
* Все consumers подписываются на одну и ту же очередь
* RabbitMQ просто раздаёт сообщения:
  * одно сообщение → одному consumer
  * по round-robin / prefetch

📌 RabbitMQ не знает про “группы”, только про очередь


## Как выглядит масштабирование в RabbitMQ
```
Queue
  ├─ Consumer #1 (app instance)
  ├─ Consumer #2 (app instance)
  └─ Consumer #3 (app instance)
```

* Добавил инстанс → добавился consumer
* Убрал инстанс → он перестал получать сообщения
* Никаких ребалансов, партиций, координации


## Самое важное различие (1 фраза)
> В Kafka масштабирование происходит через партиции и consumer group,
> в RabbitMQ — через несколько consumers на одной очереди.
