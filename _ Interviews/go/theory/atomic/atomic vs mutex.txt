Atomic
- For simple, single-variable operations (e.g., increment).
- Lock-free → very fast.

Mutex
- For complex logic or multiple variables.
- Uses locking → slower but more flexible.

Rule of thumb
- Atomic → simple + performance-critical
- Mutex → complex + shared state