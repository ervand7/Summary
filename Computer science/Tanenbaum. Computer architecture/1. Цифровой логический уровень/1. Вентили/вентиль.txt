🔑 **Вентиль** (логический элемент) — это электронное устройство, выполняющее
простые логические операции над битами (0 и 1):

* **NOT** — инверсия (НЕ)
* **AND** — логическое И
* **OR** — логическое ИЛИ
* **XOR** — исключающее ИЛИ

✅ Вентили — это строительные блоки цифровых схем: процессоров, микроконтроллеров,
памяти.

✅ Каждый вентиль состоит из нескольких **транзисторов**, которые физически
реализуют его работу.

Комбинируя вентили, можно создавать любые цифровые устройства.
