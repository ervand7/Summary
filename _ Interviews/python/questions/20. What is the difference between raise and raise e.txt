`raise`

* re-raises the current exception
* preserves the original traceback

`raise e`

* raises the exception again
* overwrites the traceback (the error appears “as if” it happened here)

Rule:

> In `except`, almost always use `raise`, not `raise e`.