It’s about event-driven orchestration without Step Functions.

> Instead of a central workflow (Step Functions), you chain Lambdas via SNS/SQS.

How it works (conceptually):

```
Lambda A → SNS topic → Lambda B → SNS topic → Lambda C
```
