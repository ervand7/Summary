1) обрезая слайс с верку ([:n]) - мы сохраняем его cap. Адрес памяти сохраняется.
Разумеется, с базовым массивом ничего не происходит.

2) Обрезая слайс снизу ([n:]), его capacity уменьшается (cap = cap - n),
но новый адрес памяти не выделяется.
Базовый массив остаётся тем же, только начало слайса смещается.

Тут мы видим отличие от append, где выделяется новый базовый массив, если мы выходим
за capacity.