🔑 **Machine Word** — это основная единица данных, с которой работает процессор.

✅ Размер слова зависит от архитектуры:

* 32-битный процессор → слово = 32 бита (4 байта),
* 64-битный процессор → слово = 64 бита (8 байт).

✅ Машинные слова используются для арифметики, логики и работы с памятью.
✅ Размер слова влияет на производительность и обработку разных типов данных.
