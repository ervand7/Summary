👉 Consumer может существовать вне consumer group.

Как это бывает:
### 1️⃣ Consumer без group.id
* Не входит ни в одну consumer group
* Offsets не хранятся в Kafka
* Ты сам управляешь:
  * offset’ами
  * partition assignment’ом
* Нет rebalancing’а

Обычно используется для:
* отладки
* админских утилит
* batch / one-off jobs


### 2️⃣ Consumer с ручным назначением partition
* Даже если `group.id` задан, можно:
  * вручную assign’ить partition
  * и тогда consumer не участвует в группе
* Kafka не перераспределяет partition


Сравнение:
| Consumer group     | Без группы  |
| ------------------ | ----------  |
| Auto rebalance     | ❌          |
| Offset storage     | ❌          |
| Scalability        | ❌          |
| Production default | ✅          |
| Debug / batch      | ✅          |


### Интервью-фраза
> Kafka consumers can operate either as part of a consumer group or independently with
manual partition assignment; only consumer groups provide automatic rebalancing
and scalable consumption.

В продакшене почти всегда используют consumer groups.
